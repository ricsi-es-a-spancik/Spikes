<UserControl x:Class="PhotosSearchWPF.View.FlickrPhotoWrapPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotosSearchWPF.View"
             xmlns:flickr="clr-namespace:FlickrNet;assembly=FlickrNet"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <ListBox x:Name="ResultsWrapPanel" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=SearchResults}" Margin="0,10,0,0" 
             Background="White" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VerticalContentAlignment="Top">
        
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        
        <ListBox.ItemContainerStyle>
            <Style TargetType="{x:Type ListBoxItem}">
                <Setter Property="Focusable" Value="False"/>
            </Style>
        </ListBox.ItemContainerStyle>
        
        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type flickr:Photo}">
                <Grid>
                    <Border BorderThickness="1" BorderBrush="LightGray" Margin="5,5,5,5">
                        <Grid Background="White">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Margin="0,5,5,0" BorderBrush="LightGray" Width="20" Height="20">
                                    <Image Source="../add.png"/>
                                </Button>
                                <Button Content=" ... " VerticalAlignment="Center" Margin="0,5,5,0" Height="20" BorderBrush="LightGray"/>
                            </StackPanel>

                            <Button Name="ThumbnailImageButton" Width="95" Height="95" Margin="10,5,5,10" Grid.Row="1" Style="{StaticResource TransparentStyle}">
                                <Image Source="{Binding ThumbnailUrl, Converter={StaticResource ThumbnailImageConverter}}"  />
                            </Button>

                            <local:TagsCollectionView Grid.Row="2" Tags="{Binding Tags}" 
                                                      SearchByTagSelectedCommand="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=SearchByTagSelectedCommand}"/>
                        </Grid>
                    </Border>

                    <Image Source="../check.png" Width="20" Height="20" HorizontalAlignment="Left"  VerticalAlignment="Top"/>
                </Grid>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
