<controls:MetroWindow x:Class="UITest_SampleApplication.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:view="clr-namespace:UITest_SampleApplication.View"
        xmlns:viewmodel="clr-namespace:UITest_SampleApplication.ViewModel"
        mc:Ignorable="d"
        Title="LoginWindow" Height="300" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance d:Type=viewmodel:LoginViewModel, IsDesignTimeCreatable=True}">
    <controls:MetroWindow.Resources>
        <DataTemplate DataType="{x:Type viewmodel:CredentialsFormViewModel}">
            <view:CredentialsForm/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:LoginProgressViewModel}">
            <view:LoginProgress/>
        </DataTemplate>
    </controls:MetroWindow.Resources>
    
    <Grid>
        <controls:TransitioningContentControl Content="{Binding CurrentViewModel}" Transition="Left" RestartTransitionOnContentChange="True"/>
    </Grid>
</controls:MetroWindow>

